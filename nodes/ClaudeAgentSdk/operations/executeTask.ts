/**
 * Execute Agent Task Operation Handler
	*
	* This module re-exports from the executeTask folder for backwards compatibility.
	* The implementation is split across focused modules in ./executeTask/
 */

export { executeTaskOperation } from './executeTask/index';
