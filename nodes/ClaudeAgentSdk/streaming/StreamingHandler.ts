/**
 * StreamingHandler public entrypoint.
 *
 * Keep this file small: the implementation lives under ./handler/ so we can
 * split it further without changing import paths.
 */

export { StreamingHandler, getMarkersForFormat, interpolateMarker } from './handler/StreamingHandler';

